var OutputSpeech, SSML, ssml,
  __bind = function(fn, me){ return function(){ return fn.apply(me, arguments); }; },
  __hasProp = {}.hasOwnProperty,
  __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

ssml = require('../util/ssml');

OutputSpeech = require('./OutputSpeech');

module.exports = SSML = (function(_super) {
  __extends(SSML, _super);

  function SSML(opts) {
    if (opts == null) {
      opts = {};
    }
    this.append = __bind(this.append, this);
    this.clear = __bind(this.clear, this);
    SSML.__super__.constructor.call(this, opts);
    if (opts.ssml != null) {
      this.append(opts.ssml);
    }
  }

  SSML.prototype.clear = function() {
    return this.ssml = "";
  };

  SSML.prototype.append = function(str) {
    return this.ssml = ssml.fromStr(str, this.ssml);
  };

  return SSML;

})(OutputSpeech);
